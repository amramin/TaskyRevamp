<div class="app-splash-screen" style="@(IsVisible ? "display:flex;" : "display:none;")">
    <div class="content">
        <span class="icon">
        <SVGCrud SVGType="SVGCrud.SVGTypeEnum.TaskyIcon"></SVGCrud>
        </span>
        <h6 class="headline-6">Loading</h6>
    </div>
</div>
@* <div class="loader-overlay" style="@(IsVisible ? "display:flex;" : "display:none;")">
    <div class="spinner"></div>
</div> *@
@code {
    [Parameter] public bool IsVisible { get; set; }
}
